apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../base

patches:
  - patch: |
     - op: add
       path: /spec/template/spec/domain/memory/guest
       value: 2Gi
    target:
      kind: VirtualMachine